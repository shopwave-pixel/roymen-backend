# Netlify configuration file for ROYMEN Fashion eCommerce
# This file ensures that client-side React Router redirects all server paths back to index.html

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build]
  publish = "dist"
  command = "npm run build"
